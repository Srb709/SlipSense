# Optional. The app works without this key by using manual entry and demo mode.
# Add this only if you want screenshot-to-slip extraction through /api/analyze-image.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional safety limit for image uploads, in megabytes.
MAX_IMAGE_UPLOAD_MB=8
